apiVersion: v1
kind: Service
metadata:
  name: smart-risk-service
spec:
  type: NodePort
  selector:
    app: smart-risk-analyzer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30020