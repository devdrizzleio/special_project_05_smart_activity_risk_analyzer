apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-risk-analyzer
spec:
  template:
    spec:
      containers:
        - name: smart-risk-analyzer
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: smart-risk-secret
                  key: MONGO_URI